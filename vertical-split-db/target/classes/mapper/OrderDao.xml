<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wxy.dao.OrderDao">
<!--    <insert id="insertOrder">
        insert into t_order(price,user_id,status) values(#{price},#{userId},#{status})
    </insert>

    <select id="selectOrderByIds" resultType="java.util.Map">
        select *
        from t_order
        where order_id
        in
        <foreach collection="ids" open="(" close=")" separator="," item="id"> #{id} </foreach>
    </select>-->


</mapper>